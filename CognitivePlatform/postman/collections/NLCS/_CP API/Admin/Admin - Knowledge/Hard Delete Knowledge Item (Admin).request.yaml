$kind: http-request
description: Permanently removes the knowledge item. Irreversible.
url: "{{baseUrl}}/api/admin/knowledge/:id"
method: DELETE
headers:
  X-Admin-Secret: "{{adminSecret}}"
pathVariables:
  - key: id
    value: ""
    description: Knowledge item ID
order: 4000
